<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Project Created</title>
    <style>
        body {
            font-family: Arial, sans-serif;
        }
        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            border: 1px solid #ccc;
            border-radius: 5px;
            background-color: #f9f9f9;
        }
        .header {
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 20px;
        }
        p {
            margin-bottom: 10px;
        }
        .link {
            color: blue;
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="container">
<<<<<<< Updated upstream
        <div class="header">A new project has been created in ProjectHUB:</div>
        <p>Project Name: <%= @project.name %></p>
        <p>Start Date: <%= custom_function_date('Start Date') %></p>
        <p>End Date: <%= custom_function_date('End Date') %></p>
        <p>Planned Go Live Date: <%= custom_function_date('Planned Go Live Date') %></p>
        <p>Function Name: <%= custom_function('Function Name') %></p>
        <p>Portfolio Category: <%= custom_function('Portfolio Category') %></p>
        <p>Priority Level: <%= custom_function('Priority Level') %></p>
        <p>Member list with role: <%= @user %> (<%= @role %>)</p>
        <% previous_members = @project.members.where.not(id: nil) %>
        <% existing_members = @project.members %>
        <% existing_members.each do |member| %>
        <% if member.user.name != @user || member.roles.first.name != @role %>
        <% role_id = MemberRole.find_by(member_id: member.id)&.role_id # Corrected typo here
            role = Role.find_by(id: role_id) %>
        <p>Previous Member: <%= "#{member.user.name} (#{role.name})" %></p>
        <% end %>
        <% end %>
        <% if @user && @role %>
        <p>New Member:  <%= "#{@user} (#{@role.name})" %></p>
        <% end %>
        <p>You have been added as a member of this project. Please log in to ProjectHUB to view and manage the project details.</p>
        <p><a href="" class="link">ProjectHUB</a></p>
        <p>Best Regards</p>
=======
        <div class="header"> Members modified in ProjectHUB::</div>
        <p>Member list with role</p>
        <% existing_members = @project.members[1..-1] %>
        <%# Display existing members and their roles %>
        <% existing_members.each do |member| %>
        <% if member.user.name != @user || member.roles.first.name != @role %>
        <% role_id = MemberRole.find_by(member_id: member.id)&.role_id 
            role = Role.find_by(id: role_id) %>
        <%= "Previous Members: #{member.user.name} (#{role.name})" %>
        <% end %>
        <% end %>

        <%# Display newly added member and their role %>
        <% if @user && @role %>
        <%= "New Member: #{@user} (#{@role.name})" %>
        <% end %>

        <p>Please log in to ProjectHUB to view and manage the project details. </p>
>>>>>>> Stashed changes
    </div>
</body>
</html>
